# ML Models

- `bert_emotion_classifier/` — Pre-trained BERT model for emotion classification (6 classes: sadness, joy, love, anger, fear, surprise)
- `content_classifier.pkl` — TF-IDF + Logistic Regression for YouTube content categorisation (trained via ml/training/train_content.py)
